<?xml version="1.0" encoding="utf-8"?>
<!--
    抽屉菜单项的背景，挂在 NavigationView 的 app:itemBackground 上。

    为什么要自己画：Material 的 itemShapeFillColor / itemShapeInsetStart 那套
    只能给菜单项一个形状，而且形状是从菜单项边缘起算的，做不出「胶囊从 18dp
    才开始」这种内缩（那是给左侧那根竖装饰线留的位置）。这里换成自己的一张
    drawable，胶囊的内缩量就完全由 or_nav_item_fill_inset 说了算。

    注意这根装饰线**不在**这张 drawable 里 —— 它由 NavItemIndicatorDecoration
    画，理由见那个类（layer-list 画不出固定宽度又不贴边的线）。
    菜单项身上只有背景和前景两个 drawable 槽位，前景被 NavigationView 自己
    拿去放水波纹了（NavigationMenuPresenter 里 itemBackground 走 setBackground、
    itemRippleColor 走 setForeground），所以这里只剩背景一个槽位可用。

    顶层必须是 ripple：一旦设了 app:itemBackground，NavigationView 就整段跳过
    默认分支，而 itemRippleColor 恰好只在那个分支里被读取 —— 反编译确认过。
    不加这一层，菜单项按压就没有任何反馈。
    用 colorControlHighlight 而不是自己新开一个色名：它是主题属性，
    深浅两套自动跟随，不必在 colors.xml / values-night 里各加一条。
-->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">
    <item android:drawable="@drawable/or_nav_item_state" />
</ripple>
